<h1>Referendas</h1>
<% if params[:type].present? %><h2><% if params[:type] == "bonds" %>Bonds<% elsif params[:type] == "issues" %>Issues<% end %></h2><% end %>
<p>Select an issue&hellip;</p>
<table class="detail-table issue-table">
    <thead>
        <tr>
            <th>Title</th>
            <th>Description</th>
            <th>Type</th>
            <th>Details</th>
        </tr>
    </thead>
    <tbody>
        <tr>
            <td>{{referenda.title}}</td>
            <td>{{referenda.desc}}</td>
            <td>{{referenda.type}}</td>
            <td><a href="{{this_will_link_to_an_issue}}">More&hellip;</a></td>
        </tr>
        <tr>
            <td>{{referenda.title}}</td>
            <td>{{referenda.desc}}</td>
            <td>{{referenda.type}}</td>
            <td><a href="{{this_will_link_to_an_issue}}">More&hellip;</a></td>
        </tr>
    </tbody>
</table>
